# 通俗视频生成 · 实现备忘

> **当前版本**：MVP-D3+（2026-06-05，P1/P2/P3 已落地）  
> **新对话**：配合 [暖桥-开发现状与速查.md](暖桥-开发现状与速查.md) 顶部「30 秒速览」使用。  
> 不下载原片；**提取 → DDG HTML/Bing 刷新 analyze → 文生图 + D3 片头 + TTS + ffmpeg** → 约 45～75 秒 MP4。  
> 与 `POST /api/explain` **独立**（口播可更深、称谓面向长辈「您」）。

---

## 1. 用户流程

1. 孩子 **分享链接** → 家长「孩子推荐」可见（**1～2 秒**成功，og/联网后台补全）。
2. 详情页可 **AI 讲给长辈听**（explain，有缓存）。
3. 点 **通俗视频生成**（孩子推荐 / 快解析 · 抖音/B 站）。
4. 生成页轮询进度；可返回，服务端继续跑（**media 阶段 D3 轮询可能较久**）。
5. 完成：16:9 播放、分享、保存相册；下方 ExplainPanel 仍可追问。
6. **排查文案**：打开 `server/data/popular_videos/reports/{job_id}.md`，**先看 §0**。

---

## 2. 后端流水线

```
prepare → analyze → media → merge → done
```

| step | progress | 模块 | 说明 |
| --- | --- | --- | --- |
| prepare | 8% | `popular_video_job` + **`cover_resolver`** | og 失败 → **B 站 API → RAWG → Bing 多查询** → 占位 |
| analyze | 28% | `web_lookup` + `popular_video_analyze` | **标签优先**检索 + **内置常识** → LLM 深入口播 |
| media | 55% | `popular_video_media` + `popular_video_intro` + `vivo_tts` | 文生图×3 ∥ **按句多次 TTS**；D3 intro |
| merge | 82% | `video_slideshow` + `video_subtitles` | intro+轮播；**按句 SRT** 硬字幕（media 阶段已生成） |
| done | 100% | `popular_video_report` | mp4 + **reports/{job_id}.md** |

### 2.1 analyze 输入材料

| 字段 | 来源 | 为空时 |
| --- | --- | --- |
| 标题 / 推荐语 | `FeedItem.title` / `summary` | LLM 材料；note 即推荐语 |
| 口令 #话题# | `share_keywords` | 分享时入库；检索 + LLM |
| 页面简介 / 正文 | og / link_preview | 抖音 **⏭ 不适用** |
| 封面 | og → Bing 图 → 占位 | Bing 图不做 OCR |
| **联网（刷新）** | `build_fresh_video_web_context` | DDG/Bing；歧义词加「游戏」 |
| 联网（入库） | 分享 enrich 的 `web_context` | 与刷新合并 |

检索词：**#话题# 标签 > 推荐语 > 标题实体**（玩梗勿用情绪词/标题字面）；见 `extract_video_search_keywords`。

内置常识（口播深入展开）：`web_lookup._KNOWN_TOPIC_BLURBS`（如剑风传奇、牢大/科比、皇室战争）；analyze User 含 `【背景常识】`。

封面链：`cover_resolver.resolve_cover`；Bing 消歧见 `web_lookup._IMAGE_QUERY_OVERRIDES`。**动漫类封面仍易失败**（Bing alt score=0）。

**平台限制（勿绕过）**：

- **百度网页搜索**：无 Cookie 返回 227 字节 HTTPS→HTTP 跳转页，**已弃用**。
- **抖音 og**：PC 未登录为扫码页，httpx 拿不到视频 og；勿上无头浏览器/Cookie。

### 2.2 口播（narration）

- **听众**：50～70 岁长辈；称谓 **「您 / 推送给您 / 分享给您」**。
- **禁止**：「推给我 / 给我推 / 孩子给我」— System 约束 + `normalize_narration_perspective()` 后处理。
- **风格**：2024–2026 家人微信口语；**须讲清作品/人物背景**（非空泛「不用纠结」）。
- **字数**：单主题约 180～320 字；**多标签玩梗**（剑风传奇+牢大）约 **240～320 字，勿短于 220 字**。
- **玩梗**：标签优先；牢大=科比梗称，须介绍 NBA 公开成就（语气尊重）；剑风传奇须介绍漫画/动画/衍生。
- **与 explain 无关**：不回写 explain 缓存。

### 2.3 文生图、D3、降级

| 项 | 说明 |
| --- | --- |
| 文生图开关 | `POPULAR_VIDEO_USE_VIVO_MEDIA` |
| D3 开关 | `POPULAR_VIDEO_USE_VIVO_INTRO` |
| 文生图 size | 默认 **`2K`**（勿 1280x720；Seedream 最小 ~1920×1920） |
| 文生图顺序 | **for 循环** 3 条独立 prompt（非 `sequential_image_generation=auto`）；1003 停；失败 **退本地配额** |
| 文生图 API | `vivo_image.generate_images`；响应 **`data.images[]`** + `usage.image_count`；组图参数已预留 |
| D3 首帧 | 首张成功文生图的 **vivo CDN**（`xuanji-llm-...`） |
| 配额 | 图 **50/日、500 总**；视频 **10/日、200 总**（内存，重启清零） |
| 降级 | 片头失败→D2；**1003 → 0 张图**，`ken_burns_cover` 仍 done；Bing 图低分→占位；**仅 TTS 失败→failed** |

### 2.4 合成

| 项 | 实现 |
| --- | --- |
| 画幅 | 1280×720 居中裁剪 16:9 |
| Ken Burns | scale eval=frame，约 1.0→1.06 |
| D3 merge | `merge_intro_slides_and_audio`：片头 ~5s + 轮播 |
| 字幕 | **按句 TTS 实测时长**写 SRT（`write_srt_from_segments`）；Win `msyh.ttc` |

### 2.5 生成报告（必看）

路径：`server/data/popular_videos/reports/{job_id}.md`（**不随 work 清理删除**）

| 章节 | 内容 |
| --- | --- |
| **§0 诊断** | 封面/og/正文/OCR/联网检索 状态+原因；口播视角检查 |
| §2 | 口播稿全文（可手改） |
| §3 | video_prompt、image_prompts |
| §4 | ANALYZE_SYSTEM + 本次 User 材料（含字段状态 `[❌ 无 …]`） |
| §5 | 联网检索原文 |
| §6 | 原始输入 + 字数 |

日志：`WbVideoGen report saved …`

### 2.6 API

| 方法 | 路径 |
| --- | --- |
| GET | `/api/video/popular/eligible/{item_id}` |
| POST | `/api/video/popular/start` |
| GET | `/api/video/popular/{job_id}/status` |
| GET | `/api/video/popular/jobs` |
| GET | `/api/video/popular/files/{job_id}.mp4` |

磁盘：成片 `data/popular_videos/{job_id}.mp4`；报告 `data/popular_videos/reports/{job_id}.md`。

---

## 3. Android

| 模块 | 路径 |
| --- | --- |
| 入口 | `DetailScreen.kt`、`PopularVideoSupport.kt` |
| 生成页 | `VideoPopularScreen.kt` — 16:9、`aspectRatio(16/9)` |
| 分享 | `ShareScreen.kt` |
| 解读 | `ExplainPanel.kt` |
| 任务列表 | `MineScreen.kt`（含 interrupted 状态） |
| 完成通知 | `PopularVideoPollWorker.kt`（离开生成页后轮询） |
| 分享 MP4 | `PopularVideoFiles.kt` |

---

## 4. 版本差异（摘要）

| 能力 | MVP-C | MVP-D3（现） |
| --- | --- | --- |
| 口播 | explain 缓存 | 独立 analyze + **您** 视角 + DDG HTML/Bing 刷新 |
| 画面 | 单封面 | D3 片头 + 3 文生图轮播 |
| 字幕 | 无 | 硬字幕 |
| 排查 | 无 | **reports/*.md §0** |

---

## 5. 配额与演示

| 资源 | 限额 | 省配额 |
| --- | --- | --- |
| 文生图 | 50/日，500 总 | `POPULAR_VIDEO_USE_VIVO_MEDIA=false` |
| 图生视频 | 10/日，200 总 | `POPULAR_VIDEO_USE_VIVO_INTRO=false` |

---

## 6. 代码索引

```
server/app/services/
  popular_video_job.py       # 主编排
  cover_resolver.py          # 封面：B 站 / RAWG / Bing
  popular_video_analyze.py   # LLM + 口播视角 + 常识注入
  web_lookup.py              # 检索 / 标签排序 / _KNOWN_TOPIC_BLURBS
  popular_video_persist.py   # Job 磁盘 jobs/*.json
  popular_video_report.py    # MD 报告 §0
  popular_video_media.py     # 文生图 + 内容策略重试
  popular_video_intro.py     # D3 + prompt 净化重试
  popular_video_prompt_safety.py
  popular_video_quota.py
  vivo_tts.py                # 按句 synthesize_wav_by_sentences
  vivo_image.py / vivo_video.py
  video_slideshow.py
  video_subtitles.py         # TimedSegment / write_srt_from_segments
  link_preview.py
  paste_intel.py             # #话题# 解析（含未闭合尾标签）
```

---

## 7. 后续

- [x] D3 片头 + 轮播
- [x] 生成时检索 + MD 报告 §0
- [x] 口播「您」视角 + 玩梗标签优先 + 内置常识加深
- [x] 字幕 / 按句 TTS 时间轴对齐（P1）
- [x] 封面链 B 站 + RAWG + Bing（P2，**动漫命中率待提升**）
- [x] Job 持久化 + Android 完成通知（P3）
- [ ] **动漫/漫画封面**：剑风传奇等 Bing score=0 → AniList/TMDB/固定图/低分兜底

总览：[暖桥-开发现状与速查.md](暖桥-开发现状与速查.md)
