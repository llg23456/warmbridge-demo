# 通俗视频生成 · 实现备忘



> **当前版本**：MVP-D2（2026-06-01）  

> 不下载整段原视频；**提取 → 一次 analyze → 文生图轮播 + TTS + ffmpeg（Ken Burns + 硬字幕）** 合成约 45～75 秒 MP4。  

> 与详情页 `POST /api/explain` **独立**，口播可更深、与文字解读不一致属预期。



---



## 1. 用户流程



1. 孩子 **分享给父母**（`ShareScreen`）→ 家长「孩子推荐」可见（分享 **1～2 秒返回成功**，标题/封面后台补全）。

2. 家长打开详情，可选 **AI 讲给长辈听**（explain，有缓存）。

3. 点 **通俗视频生成**（仅孩子推荐 / 快解析 + 抖音·B 站）。

4. **通俗视频解读** 页：蒙版 + 进度；可返回，服务端继续跑。

5. 完成后：16:9 播放、分享 MP4、保存相册；下方 **ExplainPanel** 仍可追问（与口播独立）。

6. **我的 → 通俗视频任务** 查看历史 / 进行中 / 失败。



---



## 2. 后端流水线（MVP-D2）



```

prepare → analyze → media → merge → done

```



| step | progress | 模块 | 说明 |

| --- | --- | --- | --- |

| `prepare` | 8% | `popular_video_job` + `link_preview` | og 元数据、**页面正文**（去 HTML）、封面下载（Referer）、**封面 OCR**（`vivo_ocr`，失败跳过） |

| `analyze` | 28% | `popular_video_analyze.py` | 一次蓝心 Chat → JSON：`core_keyword`、`narration`（60% 深入核心词）、`video_prompt`、`image_prompts[3]`；**不写 explain 缓存** |

| `media` | 55% | `popular_video_media` + `vivo_tts` | **TTS ∥ 顺序文生图**（for 循环最多 3 次 `vivo_image` §5）→ `narration.wav` |

| `merge` | 82% | `video_slideshow` + `video_subtitles` | 多图 Ken Burns 轮播（或封面兜底）；**SRT 硬字幕**；混 AAC |

| `done` | 100% | — | `video_url` 指向 `{job_id}.mp4` |



### 2.1 材料来源（analyze 输入）



| 字段 | 来源 |

| --- | --- |

| 标题 / 摘要 / tag | `FeedItem` |

| 页面简介 | og `page_description` |

| 页面正文 | `link_preview.extract_visible_text`（SPA 页可能仍很少） |

| 封面 OCR | 封面图 bytes → OCR |

| 联网摘要 | 分享入库时 `web_context`（DuckDuckGo/维基） |



### 2.2 文生图与降级



- 开关：`POPULAR_VIDEO_USE_VIVO_MEDIA=true`（`.env`）。

- 配额：`popular_video_quota.py` — **10 次/天、300 总量**（内存，重启清零；**API 失败会退回本地计数**）。

- 每次任务最多 **3 张**；**顺序** for 循环（非并行 3 路）；遇 **1003** 停后续；prompt 自动加 `16:9 landscape`；默认尺寸 **`2K`**（勿用 1280x720，Seedream 最小约 1920×1920 像素）。

- **静默降级**：配额用尽 / API 失败 / 开关关闭 → `slides=0` → merge 用 **封面/占位 Ken Burns**，仍烧字幕。

- 部分成功（1～2 张）→ 按成功张数轮播，**仍 done**。



### 2.3 合成细节



| 项 | 实现 |

| --- | --- |

| 画幅 | `image_to_jpeg`：**放大 + 居中裁剪** 1280×720（避免方图左右黑边） |

| 动效 | `scale` + `eval=frame` + `crop` 缓慢放大（约 1.0→1.06），替代易抖的 `zoompan` |

| 轮播 | 每图时长 = 口播总时长 / 张数；ffmpeg concat → 混音 |

| 字幕 | `video_subtitles.write_srt` 按字数分段时间轴；ffmpeg `subtitles` 烧录；字体 Win **`msyh.ttc`** |



工作目录调试文件（任务结束 `cleanup_work_dir` 会删）：`analyze.json`、`narration.srt`、`slide_*.jpg`。



### 2.4 接口



| 方法 | 路径 |

| --- | --- |

| GET | `/api/video/popular/eligible/{item_id}` |

| POST | `/api/video/popular/start` body `{ "item_id" }` |

| GET | `/api/video/popular/{job_id}/status` |

| GET | `/api/video/popular/jobs` |

| GET | `/api/video/popular/files/{job_id}.mp4` |



任务内存：`popular_video_store.py`；成片磁盘：`server/data/popular_videos/{job_id}.mp4`。



### 2.5 环境依赖



| 依赖 | 说明 |

| --- | --- |

| ffmpeg / ffprobe | PATH 必填 |

| `VIVO_APP_KEY` | analyze + TTS + 文生图 |

| `VIVO_OCR_BUSINESSID` | 封面 OCR（可选） |

| `POPULAR_VIDEO_USE_VIVO_MEDIA` | 关则 D1 式仅封面 |

| 局域网 IP | 手机访问 `video_url`；见 README §2 |



**Logcat / 服务端**：tag **`WbVideoGen`**。



---



## 3. Android 要点



| 模块 | 路径 |

| --- | --- |

| 入口 | `DetailScreen` + `PopularVideoSupport.kt` |

| 生成页 | `VideoPopularScreen.kt` — 轮询 2s、**aspectRatio(16/9)** 播放 |

| 孩子分享 | `ShareScreen.kt` — 成功条、按钮变「已发送」、防连点 |

| 解读 | `ExplainPanel.kt`（与视频 analyze **独立**） |

| 我的任务 | `MineScreen.kt` |

| 分享/相册 | `PopularVideoFiles.kt` — ClipData、`cleanupPopularVideoCache` |



---



## 4. 与 MVP-C / 全链路的差异



| 能力 | MVP-C（旧） | MVP-D2（现） |

| --- | --- | --- |

| 口播 | explain 缓存 + 二次压稿 | **独立 analyze**，单点深入 |

| 画面 | 单封面静态/loop | **3 张文生图轮播** + Ken Burns |

| 提取 | 仅 og | og + **正文 + OCR** |

| 字幕 | 无 | **硬字幕 SRT** |

| 片头视频 | 无 | **D3 未做**（`vivo_video.py` stub） |



**仍不做**：原片下载、ASR、服务端代发微信（仅系统分享 + 原链接文本）。



---



## 5. 配额与演示建议



| 资源 | 限额 | 建议 |

| --- | --- | --- |

| 文生图 | 10/日，300 总 | 约 **3 条完整轮播/天**；紧张时 `POPULAR_VIDEO_USE_VIVO_MEDIA=false` |

| 视频生成（D3） | 5/日，50 总 | 尚未接入 |



---



## 6. 已知限制



1. **占位图**：封面下载失败时仍为橙色品牌底（无中文 drawtext）。

2. **口播 ≠ 详情解读**：analyze 不回写 explain；场景不同（口播讲深 vs 摘要快读）。

3. **字幕时间**：按字数均分，非 TTS 逐字对齐；口播很长时可能略漂移。

4. **D3**：局域网 `10.x.x.x` 封面 URL vivo 云端拉不到，片头图生视频需公网或改用 img1 CDN 首帧。

5. **TTS**：超长口播可能被截断（~2048 字节）。



---



## 7. 代码索引（改功能从这里进）



```

server/app/services/

  popular_video_job.py      # 主编排

  popular_video_analyze.py  # LLM JSON analyze

  popular_video_media.py    # 顺序文生图（for 循环）

  popular_video_quota.py    # 配额

  vivo_image.py             # §5 API

  vivo_video.py             # D3 stub

  video_slideshow.py        # Ken Burns / 轮播 / mux

  video_subtitles.py        # SRT

  popular_video_cleanup.py  # 磁盘清理

  link_preview.py           # og + page_text

```



---



## 8. 后续迭代（D3+）



- [ ] **D3**：5s 图生视频片头 + 轮播（需公网首帧或 img1 CDN URL）

- [ ] 字幕与 TTS 时间轴对齐 / 真 ASR

- [ ] B 站/抖音专用封面

- [ ] Job SQLite + 完成推送



总览见 [暖桥-开发现状与速查.md](暖桥-开发现状与速查.md)。


