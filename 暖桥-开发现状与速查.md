# 暖桥 WarmBridge Demo · 开发现状与速查



> **用途**：新对话 / 新同学接手时快速恢复上下文。  

> **仓库**：`warmbridge-demo/`（Android Kotlin + FastAPI BFF）  

> **更新**：2026-06-01（通俗视频 MVP-D1/D2 已落地）



---



## 1. 已完成功能一览



| 模块 | 状态 | 说明 |

| --- | --- | --- |

| 双端角色 + 三 Tab | ✅ | 家长 / 孩子；首页、今日热点、我的 |

| 热点 Mock + 标签 | ✅ | `GET /api/tags`、`GET /api/feed?tag=`；多选标签 `\|` 连接 |

| 孩子分享 | ✅ | `POST /api/share` → **秒回成功**，og/联网摘要 **后台补全** |

| 详情 + AI 解读 | ✅ | `POST /api/explain`；`from_llm`、仅成功缓存 |

| 预测追问 Chip | ✅ | `suggested_questions` 三条口语 |

| TTS 听摘要 | ✅ | `POST /api/tts` → JSON + `audio_base64` WAV |

| 图片识梗 | ✅ | `POST /api/image/explain`（OCR + 蓝心） |

| 视频快解析 | ✅ | `POST /api/video/quickparse`（og + 解读） |

| **通俗视频生成** | ✅ **MVP-D2** | 见 §4；D3 图生视频片头 **未做**（仅 stub） |

| 温情提醒 | ✅ | WorkManager 应用内弹窗 |

| 每日 07:00 _digest | ✅ | APScheduler 刷新 Mock 浓缩条目 |



---



## 2. 技术栈



| 层 | 技术 |

| --- | --- |

| Android | Kotlin、Jetpack Compose、Material3、Retrofit/OkHttp、WorkManager、FileProvider |

| 后端 | Python 3.10+、FastAPI、httpx、websockets（TTS）、APScheduler |

| 云端 | vivo 蓝心 **Chat**、**OCR §8**、**TTS §14**、**文生图 §5**（通俗视频轮播） |

| 视频合成 | **ffmpeg** + **ffprobe**（Ken Burns、轮播拼接、**SRT 硬字幕**） |

| 数据 | 热点 Mock；分享/识图/任务 **内存**，重启 uvicorn 清空 |



---



## 3. API 速查（BFF）



| 方法 | 路径 | 作用 |

| --- | --- | --- |

| GET | `/health` | 健康检查 |

| GET | `/api/tags` | 兴趣标签 |

| GET | `/api/feed?tag=&channel=` | 热点；`channel=child\|trend` |

| GET | `/api/items/{id}` | 单条 |

| POST | `/api/explain` | 解读；`item_id` + 可选 `question` |

| POST | `/api/share` | 孩子分享（快速入库 + 后台 enrich） |

| POST | `/api/image/explain` | 识图 multipart **`file`** |

| POST | `/api/video/quickparse` | 粘贴口令快解析 |

| POST | `/api/tts` | 朗读文本 → WAV base64 |

| GET | `/api/video/popular/eligible/{item_id}` | 是否可生成通俗视频 |

| POST | `/api/video/popular/start` | `{ "item_id" }` → `job_id` |

| GET | `/api/video/popular/{job_id}/status` | 进度 / 失败 / `video_url` |

| GET | `/api/video/popular/jobs` | 我的页任务列表 |

| GET | `/api/video/popular/files/{job_id}.mp4` | 成片下载 |



文档：`http://<IP>:8000/docs`



---



## 4. 通俗视频生成（MVP-D2）流水线



**不做**：下载原片、ASR、**D3 图生视频片头**（局域网无法给 vivo 公网首帧，已预留 stub）。



```

条目(孩子推荐/快解析, 抖音/B站)

  → prepare: og + 页面正文 + 封面下载/OCR + 占位图兜底

  → analyze: 一次 LLM → core_keyword + 深度口播 + image_prompts（不回写 explain 缓存）

  → media:  顺序 vivo 文生图×3（与 TTS 并行，可降级）+ TTS → narration.wav

  → merge:  多图 Ken Burns 轮播 / 封面兜底 + 口播硬字幕 + AAC

  → done:   /api/video/popular/files/{job_id}.mp4

```



| step | 进度 | 说明 |

| --- | --- | --- |

| `prepare` | 8% | `link_preview` + `extract_visible_text`；封面 Referer 下载；`vivo_ocr` 封面 OCR（可选） |

| `analyze` | 28% | `popular_video_analyze.py` 单轮 JSON；口播 **单点深入** + 末尾引导看原视频 |

| `media` | 55% | `popular_video_media` **顺序**文生图×3（与 TTS 并行）∥ `vivo_tts`；配额/开关不足则跳过文生图 |

| `merge` | 82% | `video_slideshow`：16:9 **居中裁剪**、平滑 Ken Burns、**SRT 烧字幕** |

| `done` | 100% | 成片 URL（含局域网 IP） |



**降级（静默，不 Toast）**：文生图 0～2 张成功仍出片；全失败 → 封面 Ken Burns + 口播 + 字幕。



**日志 tag**：`WbVideoGen`（含 `merge mode=carousel|ken_burns`、`burn subtitles`、`quota`）



### 关键服务端文件



| 文件 | 作用 |

| --- | --- |

| `popular_video_job.py` | 流水线编排 |

| `popular_video_analyze.py` | 一次 LLM analyze（独立于 explain） |

| `popular_video_media.py` | **顺序**文生图（for 循环；失败退配额；1003 停后续） |

| `popular_video_quota.py` | 文生图配额 10/日、300 总量（内存） |

| `vivo_image.py` | vivo §5 文生图 |

| `video_slideshow.py` | Ken Burns / 轮播 / 混音 |

| `video_subtitles.py` | 口播 → SRT → ffmpeg 硬字幕 |

| `vivo_video.py` | **D3 stub**（`submit_vivo_video` 空实现） |

| `popular_video_cleanup.py` | work 目录清理 + mp4 保留策略 |



---



## 5. Android 关键路径



| 功能 | 文件 |

| --- | --- |

| API 根地址 | `android/local.properties` → `warmbridge.api.baseUrl=http://<IP>:8000/` |

| 详情 + 通俗视频入口 | `DetailScreen.kt`、`PopularVideoSupport.kt` |

| 生成页 | `VideoPopularScreen.kt`（**16:9** 播放器、轮询 2s） |

| 孩子分享 | `ShareScreen.kt`（秒回成功条 + 防重复发送） |

| 解读复用 | `ExplainPanel.kt`（与视频口播 **可不一致**，设计如此） |

| 我的任务 | `MineScreen.kt` |

| 分享/保存相册 | `PopularVideoFiles.kt`（ClipData 分享、离开页清 cache） |

| 主题色 | `WbBrandOrange` `#E07A3D`、`WbPageBg` `#F7F5F2` |



**真机联调**：README **§2 局域网 IP**；改 IP 后 Sync Gradle + 重装 App。



---



## 6. 环境变量（`server/.env`）



| 变量 | 说明 |

| --- | --- |

| `VIVO_APP_KEY` | 必填（Chat / OCR / TTS / 文生图） |

| `VIVO_APP_ID` | OCR businessid 备选 |

| `VIVO_OCR_BUSINESSID` | 封面 OCR、识图 |

| `VIVO_TTS_ENGINEID` / `VIVO_TTS_VCN` | TTS §14 |

| `POPULAR_VIDEO_USE_VIVO_MEDIA` | `true`/`false`；`false` 跳过文生图（省配额，仅封面轮播） |

| `VIVO_IMAGE_MODEL` | 默认 `Doubao-Seedream-4.5` |

| `VIVO_IMAGE_SIZE` | 默认 **`2K`**（勿用 1280x720；Seedream 最小约 1920×1920 像素） |
| `VIVO_IMAGE_URL` | 默认 `https://api-ai.vivo.com.cn/api/v1/image_generation`（**勿**填火山 `ark.volces.com`） |

| `WEB_SEARCH_ENABLED` | 分享/解读联网摘要 |



模板见 `server/.env.example`。



---



## 7. 启动命令



```bat

cd warmbridge-demo\server

python -m venv .venv

.venv\Scripts\activate

pip install -r requirements.txt

copy .env.example .env

uvicorn app.main:app --host 0.0.0.0 --port 8000

```



**依赖**：本机 **ffmpeg**、**ffprobe**（PATH）；Windows 字幕用 `C:\Windows\Fonts\msyh.ttc`。



---



## 8. 已知问题与现象（2026-06-01）



| # | 现象 | 原因 / 状态 |

| --- | --- | --- |

| 1 | 封面仍是橙色占位 | B 站/抖音 og 防盗链；文生图成功时画面为 AI 图，占位仅兜底 |

| 2 | 口播与详情「AI 解读」不一致 | **设计**：analyze 独立链路，不回写 explain 缓存 |

| 3 | 文生图配额易触顶 | 3 张/次 × 10 次/日；**顺序**生成 + 失败退本地配额；1003 停打；触顶静默降级封面；可关 `POPULAR_VIDEO_USE_VIVO_MEDIA`；**重启 uvicorn 会清零本地计数**（与云端可能不一致） |

| 4 | 任务 / 分享重启丢失 | Demo 内存存储 |

| 5 | `video_url` 换 Wi‑Fi 失效 | URL 含局域网 IP，换网需重生成 |

| 6 | TTS 过长截断 | 约 2048 字节上限（`vivo_tts`） |

| 7 | 多次生成后变卡 | 服务端 cleanup + App 清 `cache/wb_popular_*.mp4` |

| 8 | 字幕方框字 | ffmpeg 找不到中文字体；Win 应有微软雅黑，否则需 `server/assets/fonts/` 放字体 |

| 9 | D3 片头视频 | 未实现；需公网 URL 给 vivo 拉首帧 |



---



## 9. 相关文档索引



| 文档 | 内容 |

| --- | --- |

| [README.md](README.md) | 从零配置、IP、常见问题 |

| [通俗视频生成-实现备忘.md](通俗视频生成-实现备忘.md) | 通俗视频细节（**新对话必读**） |

| [功能优化方案-图像视频追问TTS与资讯刷新.md](功能优化方案-图像视频追问TTS与资讯刷新.md) | 产品决策 |

| [联调问题报告.md](联调问题报告.md) | 明文 HTTP、explain 缓存 |

| [vivo-aigc-接口开发速查.md](../vivo-aigc-接口开发速查.md) | 官方 API |



---



## 10. 下一步（可选）



| 优先级 | 项 | 说明 |

| --- | --- | --- |

| P1 | **D3 片头** | 公网/云服务器 + `vivo_video.py` 图生视频 5s；或 img1 CDN 作首帧 |

| P2 | 封面拉取 | B 站/抖音专用封面 API，减少占位图 |

| P3 | 字幕对齐 | 按 TTS 时间轴或 ASR 对齐，替代按字数均分 |

| P4 | Job 持久化 | SQLite + 完成通知 |



---



*维护：改通俗视频/分享逻辑后同步更新本节与《通俗视频生成-实现备忘》。*


