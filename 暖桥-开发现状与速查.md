# 暖桥 WarmBridge Demo · 开发现状与速查

> **用途**：新对话 / 新同学接手时**先读本文**，再按需打开《通俗视频生成-实现备忘》。  
> **仓库**：`warmbridge-demo/`（Android Kotlin + FastAPI BFF）  
> **更新**：2026-06-04 晚（Bing 歧义过滤 + 百科优先；通俗视频 MVP-D3 可演示）

---

## 今日现状摘要（2026-06-04）

### 已能稳定演示

| 能力 | 状态 |
| --- | --- |
| 孩子分享 → 家长「孩子推荐」 | ✅ 秒回；口令词入库 `share_keywords` |
| 通俗视频：口播 + 硬字幕 + 成片 | ✅ 独立 analyze（「您」视角）；报告 `reports/{job_id}.md` |
| 抖音链接 | ✅ 不抓 og/正文（⏭ 正常）；靠标题 + 推荐语 + 联网检索 |
| 联网检索 | ✅ 百科 / DDG HTML / Bing（**已过滤**王室偏题） |
| 封面 | ✅ og 失败 → Bing 图（消歧 + 打分）→ 不合格则 **橙色占位** |
| 磁盘 | ✅ 中间文件任务结束即删；未保存离开页 release mp4 |

### 当前主要问题（演示时注意）

| # | 现象 | 原因 / 应对 |
| --- | --- | --- |
| 1 | **文生图 1003** | vivo `Rate limit exceeded` → 0 张 AI 轮播，仅 **封面 Ken Burns** 仍 done；演示可关 `POPULAR_VIDEO_USE_VIVO_MEDIA` |
| 2 | **「皇室战争」误成 Clash 代理** | 检索词勿用裸 `Clash`；已过滤 VPN/订阅 SERP + LLM 以标题为准 + 百科/常识兜底 |
| 3 | **文生图 1003** | vivo 限流 → 0 张轮播，`ken_burns_cover` 仍 done |
| 4 | 口令 `#话题#` 为空 | 旧分享无 `share_keywords`；需 **重新分享** 或依赖标题/推荐语 |
| 5 | 百度网页搜索 | **已弃用**（227 字节跳转页） |
| 6 | 任务 / 分享数据 | 内存存储，**重启 uvicorn 清空**；`reports/*.md` 保留 |

### 口播材料（analyze 实际使用）

```
分享口令 #话题#  →  share_keywords
孩子推荐语       →  summary（note）
标题            →  title
联网检索        →  百科 / DDG / Bing（过滤后）
LLM 常识展开     →  检索空时允许，禁编日期/采访
❌ 不用         →  抖音 og、page_text
```

---

## 新对话 30 秒速览

| 问 | 答 |
| --- | --- |
| 通俗视频做到哪了？ | **MVP-D3**：prepare → analyze → media → merge → done |
| 改口播 / 提示词看哪？ | `server/data/popular_videos/reports/{job_id}.md` — **§0** + §2 + §4 |
| 日志 tag？ | `WbVideoGen`、`web_lookup` |
| 启动后端？ | `cd warmbridge-demo\server` → `uvicorn app.main:app --host 0.0.0.0 --port 8000` |
| 真机 API？ | `android/local.properties` → `warmbridge.api.baseUrl=http://<IP>:8000/` |
| 口播称谓？ | **「您 / 推送给您」** |
| 配额紧张？ | 1003 → Ken Burns 仍 done；关 `POPULAR_VIDEO_USE_VIVO_MEDIA` / `_INTRO` |

---

## 1. 已完成功能一览

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 双端角色 + 三 Tab | ✅ | 家长 / 孩子 |
| 孩子分享 | ✅ | 秒回 + 后台 enrich |
| 详情 + AI 解读 | ✅ | explain 缓存 |
| **通俗视频生成** | ✅ **MVP-D3** | 片头 + 轮播 + 口播 + 字幕 + 报告 |
| TTS / OCR / 识图 | ✅ | vivo 网关 |

---

## 2. 技术栈

| 层 | 技术 |
| --- | --- |
| Android | Kotlin、Compose、Retrofit |
| 后端 | FastAPI、httpx、APScheduler |
| 云端 | vivo Chat / OCR / TTS / 文生图 / 图生视频 |
| 联网 | 百科优先（歧义词）→ DDG HTML → Bing（**SERP 过滤**） |
| 视频 | ffmpeg Ken Burns + 硬字幕 |

---

## 3. API 速查

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| POST | `/api/share` | 孩子分享 |
| POST | `/api/video/popular/start` | 开始通俗视频 |
| GET | `/api/video/popular/{job_id}/status` | 进度 |
| POST | `/api/video/popular/{job_id}/release` | 未保存离开 → 删 mp4 |
| GET | `/api/video/popular/files/{job_id}.mp4` | 成片 |

文档：`http://<IP>:8000/docs`

---

## 4. 通俗视频流水线

```
prepare → analyze → media → merge → done
```

| step | 说明 |
| --- | --- |
| prepare | og → **Bing 图**（`Clash Royale…` 消歧 + alt 打分）→ 占位；抖音不抓 og |
| analyze | jieba 实体 + 口令词 → **百科/DDG/Bing** → LLM 口播 |
| media | 顺序文生图×3 ∥ TTS；1003 → 0 图 |
| merge | 片头+轮播 / **ken_burns_cover** + 硬字幕 |

**报告**：`server/data/popular_videos/reports/{job_id}.md` §0 诊断

**关键文件**：`popular_video_job.py`、`web_lookup.py`、`popular_video_analyze.py`、`popular_video_report.py`

---

## 5. 环境变量

| 变量 | 说明 |
| --- | --- |
| `VIVO_APP_KEY` | 必填 |
| `WEB_SEARCH_ENABLED` | 通俗视频联网检索 |
| `POPULAR_VIDEO_USE_VIVO_MEDIA` | `false` 跳过文生图 |
| `POPULAR_VIDEO_USE_VIVO_INTRO` | `false` 跳过 D3 片头 |
| `POPULAR_VIDEO_MP4_IDLE_SEC` | 未保存 mp4 保留秒数（默认 3600） |
| `POPULAR_VIDEO_MP4_MAX_KEEP` | 最多保留成片（默认 5） |

模板：`server/.env.example`

---

## 6. 启动

```bat
cd warmbridge-demo\server
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

依赖：**ffmpeg**、**ffprobe**；Win 字幕 `msyh.ttc`

---

## 7. 文档索引

| 文档 | 内容 |
| --- | --- |
| [通俗视频生成-实现备忘.md](通俗视频生成-实现备忘.md) | 流水线细节 |
| [README.md](README.md) | 配置、FAQ |
| [vivo-aigc-接口开发速查.md](../vivo-aigc-接口开发速查.md) | 官方 API |

---

## 8. 下一步（可选）

| 优先级 | 项 |
| --- | --- |
| P1 | 字幕与 TTS 时间轴对齐 |
| P2 | 专用游戏/视频封面 API（替代 Bing 图） |
| P3 | Job 持久化 + 完成通知 |

---

*维护：改通俗视频/检索/报告后同步本文与《通俗视频生成-实现备忘》。*
