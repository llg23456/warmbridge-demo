# 暖桥 WarmBridge Demo · 开发现状与速查

> **用途**：新对话 / 新同学接手时**先读本文**，再按需打开《通俗视频生成-实现备忘》。  
> **仓库**：`warmbridge-demo/`（Android Kotlin + FastAPI BFF）  
> **更新**：2026-06-06（解读服务者口吻 · 词语小抄编号 · 口播禁分点）

---

## 今日现状摘要（2026-06-06）

### 已能稳定演示

| 能力 | 状态 |
| --- | --- |
| 孩子分享 → 家长「孩子推荐」 | ✅ 秒回；`#话题#` 入库 `share_keywords`（含未闭合尾标签如 `#牢大`） |
| 通俗视频：口播 + 硬字幕 + 成片 | ✅ 按句 TTS 对齐字幕；玩梗视频 **240～320 字** 深入口播；报告 `reports/{job_id}.md` |
| 抖音链接 | ✅ 不抓 og/正文（⏭ 正常）；**标签优先于标题**（如剑风传奇/牢大，非「赴约的鹤」字面） |
| 联网检索 + 常识 | ✅ **联网新鲜摘要优先**（`fetch_fresh_context_for_topic` + tools）；本地词条仅检索不足时补充 |
| 封面 | ⚠️ **部分命中**：B 站 API / RAWG（游戏）/ Bing 多查询；**动漫/漫画类常 score=0 → 占位** |
| 通俗视频 Job | ✅ **磁盘持久化** `data/popular_videos/jobs/*.json`；Android **完成通知**（WorkManager） |
| 通俗视频字幕 | ✅ **一句一条**（≤28 字；超长仅逗号/顿号切分，不拆词） |
| AI 解读追问 | ✅ 按钮下方 **追问记录** 堆叠；`follow_up_answer` 直答；**tools 联网**（`vivo_chat_tools.py`） |
| AI 解读文案风格 | ✅ **plain_summary** 服务者口吻、禁「从标题/关键词看」；**glossary** `1. 词：解释` 分行（`normalize_glossary`） |
| 通俗视频口播风格 | ✅ **连贯叙述**、禁 1.2.3. 分点；兜底禁粘 Bing 检索（`normalize_narration_prose`） |
| 磁盘 | ✅ 中间 work 任务结束即删；未保存离开页 release mp4 |

### 当前主要问题（演示时注意）

| # | 现象 | 原因 / 应对 |
| --- | --- | --- |
| 1 | **封面占位（剑风传奇等）** | Bing 图 alt 与动漫关键词不匹配 → `score=0`；见 `pv-3edfb1a23f62` §0。**待办**：动漫封面 API / 百科配图 / 放宽打分或固定素材库 |
| 2 | **文生图 1003** | vivo 限流 → 0 张轮播，Ken Burns 仍 done；关 `POPULAR_VIDEO_USE_VIVO_MEDIA` 演示 |
| 3 | **「皇室战争」误成 Clash 代理** | 已过滤 VPN/订阅 SERP + 标签优先 + 内置常识 |
| 4 | 口令 `#话题#` 不全 | **旧分享**无完整 `share_keywords`；改代码后需 **重新分享** 才入库全部标签 |
| 5 | 百度网页搜索 | **已弃用**（227 字节跳转页） |
| 6 | **分享条目**仍内存 | 重启 uvicorn **孩子推荐列表清空**；通俗视频 **Job 可恢复**（running→interrupted） |

### 口播材料（analyze 实际使用）

```
分享口令 #话题#  →  share_keywords
孩子推荐语       →  summary（note）
标题            →  title
联网检索        →  百科 / DDG / Bing（过滤后）
LLM 常识展开     →  检索空时允许，禁编日期/采访
❌ 不用         →  抖音 og、page_text
```

---

## 新对话 30 秒速览

| 问 | 答 |
| --- | --- |
| 通俗视频做到哪了？ | **MVP-D3+**：P1 字幕对齐 · P2 封面链 · P3 Job 持久化+通知 |
| 改口播 / 提示词看哪？ | `reports/{job_id}.md` — **§0** + §2（口播）+ §4（Prompt） |
| 玩梗视频口播逻辑？ | **#话题# 标签 > 推荐语 > 标题**；内置常识见 `web_lookup._KNOWN_TOPIC_BLURBS` |
| 日志 tag？ | `WbVideoGen`、`web_lookup` |
| 启动后端？ | `cd warmbridge-demo\server` → `uvicorn app.main:app --host 0.0.0.0 --port 8000` |
| 真机 API？ | `android/local.properties` → `warmbridge.api.baseUrl=http://<IP>:8000/` |
| 口播称谓？ | **「您 / 推送给您」**；多实体约 **240～320 字** |
| 配额紧张？ | 1003 → Ken Burns 仍 done；关 `POPULAR_VIDEO_USE_VIVO_MEDIA` / `_INTRO` |
| 离开生成页？ | 服务端继续跑；完成后 **系统通知**（需通知权限） |
| 追问在哪显示？ | **「带着追问重新解读」按钮下方**「追问记录」，不替换顶部摘要 |
| 测 tools 权限？ | `cd server` → `python test_tools.py` |
| 解读/口播文案规则？ | 均 **联网优先**：`fetch_fresh_context_for_topic` → LLM **tools**；explain `vivo_llm` / 口播 `popular_video_analyze` |
| 旧条目仍显示推理腔？ | explain **有缓存** → 详情页 **重新解读** 或清 `explain` 缓存 |

---

## 1. 已完成功能一览

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 双端角色 + 三 Tab | ✅ | 家长 / 孩子 |
| 孩子分享 | ✅ | 秒回 + 后台 enrich |
| 详情 + AI 解读 | ✅ | explain 缓存 |
| **通俗视频生成** | ✅ **MVP-D3** | 片头 + 轮播 + 口播 + 字幕 + 报告 |
| TTS / OCR / 识图 | ✅ | vivo 网关 |

---

## 2. 技术栈

| 层 | 技术 |
| --- | --- |
| Android | Kotlin、Compose、Retrofit |
| 后端 | FastAPI、httpx、APScheduler |
| 云端 | vivo Chat / OCR / TTS / 文生图 / 图生视频 |
| 联网 | 百科优先（歧义词）→ DDG HTML → Bing（**SERP 过滤**） |
| 视频 | ffmpeg Ken Burns + 硬字幕 |

---

## 3. API 速查

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| POST | `/api/share` | 孩子分享 |
| POST | `/api/video/popular/start` | 开始通俗视频 |
| GET | `/api/video/popular/{job_id}/status` | 进度 |
| POST | `/api/video/popular/{job_id}/release` | 未保存离开 → 删 mp4 |
| GET | `/api/video/popular/files/{job_id}.mp4` | 成片 |

文档：`http://<IP>:8000/docs`

---

## 4. 通俗视频流水线

```
prepare → analyze → media → merge → done
```

| step | 说明 |
| --- | --- |
| prepare | og → **`cover_resolver`**（B 站 API → RAWG → Bing 多查询）→ 占位 |
| analyze | 标签优先检索 + **内置常识** → LLM 深入口播（玩梗 240～320 字） |
| media | 顺序文生图×3 ∥ **按句多次 TTS**；1003 → 0 图 |
| merge | 片头+轮播 / ken_burns + **按句 SRT 硬字幕** |

**报告**：`server/data/popular_videos/reports/{job_id}.md` §0 诊断

**关键文件**：`popular_video_job.py`、`cover_resolver.py`、`web_lookup.py`、`popular_video_analyze.py`、`vivo_tts.py`、`video_subtitles.py`、`popular_video_persist.py`

---

## 5. 环境变量

| 变量 | 说明 |
| --- | --- |
| `VIVO_APP_KEY` | 必填 |
| `WEB_SEARCH_ENABLED` | 通俗视频联网检索 |
| `RAWG_API_KEY` | 游戏类封面（可选；无则降级 Bing） |
| `POPULAR_VIDEO_USE_VIVO_MEDIA` | `false` 跳过文生图 |
| `POPULAR_VIDEO_USE_VIVO_INTRO` | `false` 跳过 D3 片头 |
| `POPULAR_VIDEO_MP4_IDLE_SEC` | 未保存 mp4 保留秒数（默认 3600） |
| `POPULAR_VIDEO_MP4_MAX_KEEP` | 最多保留成片（默认 5） |

模板：`server/.env.example`

---

## 6. 启动

```bat
cd warmbridge-demo\server
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

依赖：**ffmpeg**、**ffprobe**；Win 字幕 `msyh.ttc`

---

## 7. 文档索引

| 文档 | 内容 |
| --- | --- |
| [通俗视频生成-实现备忘.md](通俗视频生成-实现备忘.md) | 流水线细节 |
| [README.md](README.md) | 配置、FAQ |
| [vivo-aigc-接口开发速查.md](../vivo-aigc-接口开发速查.md) | 官方 API |

---

## 8. 近期已完成 / 待办

| 项 | 状态 |
| --- | --- |
| P1 字幕与按句 TTS 时间轴对齐 | ✅ `synthesize_wav_by_sentences` + `write_srt_from_segments` |
| P2 封面链（B 站 + RAWG + Bing） | ✅ 已接入；**动漫/漫画封面命中率低** → 待优化 |
| P3 Job 持久化 + 完成通知 | ✅ `jobs/*.json` + `PopularVideoPollWorker` |
| 玩梗视频标签优先 + 口播加深 | ✅ `extract_video_search_keywords` + `_KNOWN_TOPIC_BLURBS` |
| **封面：动漫/梗图** | ⏳ 如剑风传奇 Bing `score=0`；候选：AniList/TMDB/固定配图/文生图缩略 |

### 封面失败速查（`pv-3edfb1a23f62`）

- 已试：`剑风传奇`、`赴约的鹤`；跳过 `牢大`（敏感）
- 现象：Bing 返回候选但 **alt 打分全 0** → 橙色占位
- 口播/轮播/片头：**正常**（约 334 字，含剑风传奇 + 科比介绍）

---

*维护：改通俗视频/检索/报告后同步本文与《通俗视频生成-实现备忘》。*
